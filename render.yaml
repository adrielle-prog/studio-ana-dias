services:
  - type: web
    name: studiojuliadias
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 10000
      - key: WHATSAPP_SIMULATION
        value: "true"
      - key: GOOGLE_CALENDAR_SIMULATION
        value: "true"
